/**
 * Deliberately left unreferenced so the bundled RepoLens demo can show how a
 * real audit names possible dead code while still requiring human verification.
 */
export function LegacyBanner() {
  return <aside>Legacy deployment window</aside>;
}
